apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.db_name }}-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.db_storage_request }}

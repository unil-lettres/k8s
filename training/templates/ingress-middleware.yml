apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: buffering
  namespace: {{ .Values.namespace }}
spec:
  buffering:
    maxRequestBodyBytes: 209715200 # 200Mo
    maxResponseBodyBytes: 0
